# First NodeJS REST API Project - Shop

### Install Required Dependecies

* `npm init` to Initialize the package.json file
* Install packages to the project `npm install --save express nodemon body-parser morgan`